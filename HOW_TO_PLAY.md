# 遊び方

## 起動手順

1. サーバーを起動する

   ```
   cd server
   g++ -std=c++17 -O2 main.cpp GameServer.cpp GameState.cpp Protocol.cpp WebSocketServer.cpp Sha1.cpp Base64.cpp -o game_server.exe -lws2_32
   ./game_server.exe
   ```

   (`make` が使える環境では `cd server && make && ./game_server.exe` でも可)

   `Shooting game server listening on ws://localhost:8765` と表示されれば起動成功です。

2. `index.html` をダブルクリックしてブラウザで開く

   スタートボタンが「接続中...」→「スタート」に変われば、サーバーへの接続完了です。
   「サーバーに接続できません」と表示される場合はサーバーが起動していません。手順1を確認してください。

## 操作方法

| キー | 動作 |
|---|---|
| ↑ / W | 上に移動 |
| ↓ / S | 下に移動 |
| ← / A | 左に移動 |
| → / D | 右に移動 |
| スペース | 攻撃(弾を発射) |

スペースキーは押しっぱなしで連射できます。

## 遊び方の流れ

1. タイトル画面で「スタート」ボタンを押すとゲーム開始
2. 自機(水色の三角)を操作して敵(赤・紫の四角)を弾で倒すとスコア加算
   - 通常の敵(赤): 100点、1発で撃破
   - 硬い敵(紫): 300点、3発で撃破
3. 敵や敵弾に当たるとライフが1減る(被弾後は一定時間無敵になり、自機が点滅)
4. ライフが0になるとゲームオーバー画面が表示される
5. 「もう一度」ボタンでスコア・ライフをリセットして再挑戦できる

## 画面の見方

- 左上: 現在のスコア
- 右上: 残りライフ
